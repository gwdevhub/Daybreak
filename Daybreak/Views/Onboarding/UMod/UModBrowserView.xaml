<UserControl x:Class="Daybreak.Views.Onboarding.UMod.UModBrowserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Daybreak.Views.Onboarding.UMod"
             xmlns:controls="clr-namespace:Daybreak.Controls"
             x:Name="_this"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <controls:ChromiumBrowserWrapper AddressBarReadonly="True"
                                         ControlsEnabled="False"
                                         Address="{Binding ElementName=_this, Path=DataContext, Mode=OneWay}"
                                         CanNavigate="True"
                                         CanDownloadFiles="True"
                                         DownloadsDirectory="uMod/Mods"
                                         DownloadingFile="ChromiumBrowserWrapper_DownloadingFile"
                                         DownloadedFile="ChromiumBrowserWrapper_DownloadedFile"></controls:ChromiumBrowserWrapper>
    </Grid>
</UserControl>
