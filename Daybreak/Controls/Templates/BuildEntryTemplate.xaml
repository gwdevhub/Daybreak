<UserControl x:Class="Daybreak.Controls.BuildEntryTemplate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Daybreak.Controls"
             xmlns:buttons="clr-namespace:Daybreak.Controls.Buttons"
             mc:Ignorable="d" 
             xmlns:controls="clr-namespace:Daybreak.Controls"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <buttons:HighlightButton 
            FontSize="16" 
            Foreground="{StaticResource MahApps.Brushes.ThemeForeground}" 
            HighlightColor="{StaticResource MahApps.Brushes.Accent}"
            Title="{Binding Name}" 
            HorizontalAlignment="Stretch" 
            VerticalAlignment="Stretch"
            VerticalContentAlignment="Center"
            Clicked="HighlightButton_Clicked"
            Height="40"></buttons:HighlightButton>
        <buttons:BinButton Width="30" Height="30" HorizontalAlignment="Right" Clicked="BinButton_Clicked"></buttons:BinButton>
        <Rectangle 
            Height="1"
            Fill="{StaticResource MahApps.Brushes.ThemeForeground}"
            VerticalAlignment="Bottom"></Rectangle>
    </Grid>
</UserControl>
