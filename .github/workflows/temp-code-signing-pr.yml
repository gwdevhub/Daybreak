# Temporary workflow for validating Windows code signing in this PR.
# Delete this file after the one-off validation run.
name: Temporary Code Signing PR Validation

on:
  pull_request:
    branches:
      - master

jobs:
  cd:
    uses: ./.github/workflows/cd-template.yml
    with:
      publishRelease: false
      signBinaries: true
    secrets: inherit
